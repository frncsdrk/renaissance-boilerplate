'use strict';

define(
    [
        'renaissance'
    ]
    , function(renaissance) {
        console.log('in page');
        
        function init() {
            //
        }

        return init;
    }
);
